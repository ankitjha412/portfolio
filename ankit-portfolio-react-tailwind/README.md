# Ankit Jha — Portfolio (React + Tailwind + Vite)

## Local Setup
```bash
npm install
npm run dev
```

## Customize
- Replace links in `Hero`, `Contact`, and `src/data/projects.js`.
- Add images into `src/assets/` and use them in `ProjectCard` if needed.
- Edit colors in `tailwind.config.js`.
